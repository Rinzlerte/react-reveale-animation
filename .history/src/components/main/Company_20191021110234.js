import React from 'react';

const Company = () => {
    return (
        <div style={{width:`50%`, height:`100%`,backgroundColor: `green`, overflow: `hidden`, position: `absolute`}}>
            
        </div>
    );
};

export default Company;