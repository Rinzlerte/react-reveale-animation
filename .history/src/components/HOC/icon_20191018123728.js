import React from 'react';

const icon = () => {
    const img = 
    return (
        <div style={{width: "60px", height : "60px", backgroundImage: "url(`${img}`)"}}>
            
        </div>
    );
};

export default icon;